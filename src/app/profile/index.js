import React from "react";
import {Title, Panel} from "rsi-react-web-components";

const Profile = (props) => {
  return (
    <Panel>
      <Title>Profile</Title>
    </Panel>
  )
}

export default Profile;
